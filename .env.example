# This app currently ships its Supabase URL and anon key directly in
# src/lib/supabase.js rather than through env vars — nothing needed here
# for local development. Kept as a placeholder in case you move those to
# env vars later (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY).
